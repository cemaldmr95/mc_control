#include "park.h"
#include"math.h"
int theta=0;
int park(float alpha, float beta,float theta, float* i_d, float* i_q){
    *i_d = (alpha*sin(theta))+(beta*cos(theta));
    *i_q = (alpha*cos(theta))
}
iq = alpha*cos(theta)-beta*sin(theta);
id = alpha*sin(theta)+beta*cos(theta);
